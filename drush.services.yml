services:
  asset_status.commands:
    class: Drupal\asset_status\Commands\AssetStatusCommands
    arguments:
      - '@entity_type.manager'
      - '@asset_status.stale_monitor'
    tags:
      - { name: drush.command }
